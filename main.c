#include <stdio.h>

int main() {
    int hours, minutes, seconds;

    printf("Enter hours: ");
    scanf("%d", &hours);

    printf("Enter minutes: ");
    scanf("%d", &hours);

    printf("Enter seconds: ");
    scanf("%d", &hours);

    printf("Number = %d",hours);

    return 0;
}
